const KEY="sab-tools-pending-v1";export const isOnline=()=>navigator.onLine;export function readPending(){try{return JSON.parse(localStorage.getItem(KEY)||"[]")}catch{return[]}}export function queueSave(item){const list=readPending();list.push({...item,queuedAt:Date.now()});localStorage.setItem(KEY,JSON.stringify(list));return list.length}export function clearPending(){localStorage.removeItem(KEY)}export async function syncPending(saveFns){const list=readPending();if(!list.length||!navigator.onLine)return 0;let done=0;for(const item of list){try{await saveFns[item.type](item);done++}catch{break}}if(done===list.length)clearPending();else localStorage.setItem(KEY,JSON.stringify(list.slice(done)));return done}