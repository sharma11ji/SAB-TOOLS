import{addDoc,collection,deleteDoc,doc,getDocs,serverTimestamp,query}from"firebase/firestore";import{db}from"./firebase";const guard=()=>{if(!db)throw new Error("Firebase is not configured. Add your VITE_FIREBASE_* values.")};export async function saveHistory(uid,data){guard();return addDoc(collection(db,"users",uid,"history"),{...data,createdAt:serverTimestamp(),updatedAt:serverTimestamp()})}export async function listHistory(uid){guard();const s=await getDocs(query(collection(db,"users",uid,"history")));return s.docs.map(d=>({id:d.id,...d.data()}))}export async function removeHistory(uid,id){guard();return deleteDoc(doc(db,"users",uid,"history",id))}